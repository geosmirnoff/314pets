import dialog from './CurrentDialog.module.css';

const CurrentDialog = (props) => {
    return (
        <div className={dialog.wrapper}>
            <p>Hi</p>
            <p>Hi</p>
        </div>
    );
}

export default CurrentDialog;